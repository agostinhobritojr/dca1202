#include <stdio.h>
#include <stdlib.h>

int main(){
  int **x;
  int nl, nc;
  printf("digite nl: ");
  scanf("%d", &nl);
  printf("digite nc: ");
  scanf("%d", &nc);

  x = malloc(n * sizeof (int));

  free(x);
  return 0;
}
